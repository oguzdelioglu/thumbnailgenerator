<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 630">
  <defs>
    <linearGradient id="bg" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#2e1065"/>
      <stop offset="100%" style="stop-color:#050505"/>
    </linearGradient>
    <filter id="glow">
      <feGaussianBlur stdDeviation="3" result="coloredBlur"/>
      <feMerge>
        <feMergeNode in="coloredBlur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
  </defs>
  
  <!-- Background -->
  <rect width="1200" height="630" fill="url(#bg)"/>
  
  <!-- Decorative circles -->
  <circle cx="900" cy="100" r="200" fill="#8b5cf6" opacity="0.1"/>
  <circle cx="300" cy="500" r="150" fill="#38bdf8" opacity="0.1"/>
  
  <!-- Icon -->
  <text x="600" y="280" font-size="150" text-anchor="middle" filter="url(#glow)">🎥</text>
  
  <!-- Title -->
  <text x="600" y="400" font-family="Inter, Arial, sans-serif" font-size="64" font-weight="800" fill="white" text-anchor="middle" filter="url(#glow)">THUMBNAIL STUDIO</text>
  
  <!-- Subtitle -->
  <text x="600" y="470" font-family="Inter, Arial, sans-serif" font-size="28" fill="#94a3b8" text-anchor="middle">AI-Powered YouTube Thumbnail Prompt Generator</text>
  
  <!-- Domain -->
  <text x="600" y="580" font-family="JetBrains Mono, monospace" font-size="20" fill="#8b5cf6" text-anchor="middle">thumbnail.odel.dev</text>
</svg>
